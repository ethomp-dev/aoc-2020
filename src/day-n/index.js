const { parseInput, logSolution } = require('../util')

const input = parseInput(__dirname + '/input.txt')

const PartOne = () => {
  return ''
}

const PartTwo = () => {
  return ''
}

logSolution('Day n', {
  1: PartOne(),
  2: PartTwo(),
})
